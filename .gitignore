# --- Rust build artifacts ---
/target

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- vendored reference (upstream clone, has its own .git) ---
/repo/

# --- large weights (TimesFM 3.0 checkpoint, 1.32 GB) ---
/ckpt/

# --- comparison pipeline outputs (regeneratable) ---
/out_official/
/out_official*/
/out_rust/
/out_rust*/
/ckpt_*/
out.csv
out_official_sc.log
out_rust_sc.log
*.log

# --- raw source datasets (large; reproducible from public sources) ---
# Pulled from zhouhaoyi/ETDataset and laiguokun/multivariate-time-series-data;
/data/ETTh*.csv
/data/ETTm*.csv
/data/electricity*.csv
/data/exchange*.csv
/data/solar*.csv
/data/traffic*.csv

# --- benchmark source CSVs / JSON pulled in during eval (large, local-only) ---
/data/features.csv
/data/walmart_train.csv
/data/weather_beijing_full.json

# --- per-scenario trimmed CSVs (regenerated by tools/gen_scenarios*.py) ---
/data/scenarios/

# --- workspace-local agent memory (per-machine, never committed) ---
/.workbuddy/

# --- report pipeline intermediates (regeneratable via tools/report_full.py) ---
# The canonical deliverables are full_report.md (and the .html preview).
# Everything else below is feeding data, scratch JSON, or test logs.
/report/bench.json
/report/cargo_test.txt
/report/full_report.html
/report/full_summary.json
/report/matrix_v2.md
/report/noise_floor.json
/report/summary.json
/report/summary_*.json
/report/windows_eval.md

# --- editor / OS ---
.DS_Store
*.swp
/scratch/

# --- model publishing workspace (staged repos for HF / ModelScope) ---
/publish/